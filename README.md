# Screenshots

Web-preview screenshots of Free Clinic Companion at a 390x844 viewport (2x), referenced by the README on `main`. They live on this branch because Snack's Git import fails on repositories that contain image files.
